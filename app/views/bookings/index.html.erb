<%# <% @bookings.each do |booking| %>

	<%# booking.title %>
	<%# link_to "Show", booking %>
	
	<%# Render _shared partial%>
	<%# render 'bookings/shared', booking: booking %>

<%# end %>

<%= link_to "Make a booking", new_booking_path %>

<%= link_to "view", listing_path(listing) %>